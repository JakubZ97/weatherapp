import React from 'react'
import Form from './Form'

function Header() {
  return (
    <header>
        <h1>
          WEATHER APP
        </h1>
        <Form />
    </header>
  )
}

export default Header